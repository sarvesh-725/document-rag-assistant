[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    integration: requires configured PostgreSQL, Redis, Qdrant, and external ingestion services
